"""${UPPER}(LANG) to ${UPPER}(TOLANG) Converter ${UPPER}(STYLE) ${UPPER}(NC) test

Testing suite for converter $(LANG) to $(TOLANG) $(NC) in the $(STYLE) style.

"""

from nose.tools import eq_
from tests import parse_convert_write


def test_$(STYLE)():
    """$(LANG).converter.$(TOLANG).$(STYLE).$(NC): """
    t1, t2 = parse_convert_write(__file__, 'file.$(LANG)', 'file', '$(STYLE)', '$(TOLANG)')
    eq_(t1, t2)
