"""{LANG}: {STYLE} writer {NP} test

Testing suite for writing {LANG} in the {STYLE} style.

"""

import lexor.core.elements as core
from lexor.command.test import compare_with


def test_{style}():
    """{lang}.writer.{style}.{np}: """
    doc = core.Document('{lang}', '{style}')
    #compare_with(str(doc), EXPECTED)
